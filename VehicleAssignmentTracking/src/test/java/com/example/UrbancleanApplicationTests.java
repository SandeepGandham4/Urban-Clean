package com.example;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class UrbancleanApplicationTests {

	@Test
	void contextLoads() {
		// This method is intentionally empty.
		// It serves as a basic smoke test to ensure that the Spring application context
		// loads successfully without throwing any exceptions.
		// If the context fails to load, the test will automatically fail,
		// indicating a configuration issue or a missing component.
		// No specific assertions are needed here, as the success of context loading
		// is the primary objective of this test.
	}

}
